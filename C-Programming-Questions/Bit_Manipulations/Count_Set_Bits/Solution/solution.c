#include <stdio.h>

//count set bits without using loop
int count_set_bits(unsigned int a) {
  return ;
}

int main() {
  unsigned int a = 0b11010110;
  printf("%d\n", count_set_bits(a));
  return 0;
}
